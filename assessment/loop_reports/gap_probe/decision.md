# Gap Probe — 決策報告(哪個缺口值得開下一門課)

日期:2026-07-17 · 探測對象:時序模型(A)、真實資料增強(B)、QAT 實作(C)
方法:S2(目標客群)、S3(程度較弱)以現行教材應考 30 分探測卷,嚴格批改 + 轉移分析。

## 1. 分數與訊號

| 節 | S2 | S3 | 分數訊號 | 質性訊號(更關鍵) |
|----|----|----|----------|--------------------|
| A 時序 | 10/10 | 10/10 | 無失分 | 兩人自述「半靠拼湊」;A2 的 stateful export 純屬猜測(Day 27 只教了無狀態情形);LLM 學生轉移成功之處,真人未必 |
| B 增強 | 10/10 | 10/10 | 無失分 | 高信心作答:Day 28c 的標籤幾何 + Day 28b 的誠實評估規則足以讓學生**自行推導**,含安全關鍵的「翻轉必須把轉向角變號」 |
| C QAT | 9.5/10 | 9.5/10 | **唯一真實失分**(C1 漏 static 對照) | 兩人明確承認「不懂梯度如何穿過捨入階梯」(STE)、說不出任何 QAT API;S3:「C 最迷失」;S2:「C 準備最薄」 |

危險型(自信但錯)錯誤:0 —— 兩人都誠實標注了猜測,未出現會「悄悄做出錯模型」的誤解。

## 2. 決策規準逐項評分

| 規準 | A 時序 | B 增強 | C QAT |
|------|--------|--------|-------|
| 失分嚴重度 / 知識真空 | 中(滿分但低信心) | 低(可自行推導) | **高**(唯一失分 + 自承不懂機制) |
| 對課程目標(自駕推論模型)關鍵性 | 高(真實駕駛模型吃時序) | 中(最危險誤解已被現有教材防住) | **高**(直接接在 train→export→quantize 主軸上;量化失準 = 部署的模型變差) |
| 先修契合度 | 中(需序列資料、stateful export 等新機件) | 高 | **完美**(Day 28 概念 + Day 28c 的回歸器現成可用) |
| CPU 可評分課程可行性 | 中(工程量大) | 高(但需求低) | **已驗證**(見下) |

## 3. 可行性實驗(已跑通)

手刻 symmetric fake-quant + straight-through estimator,在 Day 28c 道路資料(攤平 + MLP)上:

| bits | float MAE | PTQ MAE | QAT MAE |
|------|-----------|---------|---------|
| 3 | 0.0065 | 0.0458(7× 劣化) | **0.0102**(恢復 78% 損失) |
| 4 | 0.0065 | 0.0166 | 0.0101 |
| 8 | 0.0065 | 0.0067 | 0.0061 |

CPU 數十秒內完成;8-bit 行印證 Day 28「int8 通常可接受」,3-bit 行讓 QAT 的價值肉眼可見。
不依賴 `torch.ao` 遷移中的 API(torch 2.13 已對其發 deprecation 警告),全部手刻 —— 也正面
回答了 S3 的具體疑問「梯度怎麼過 round()」。

## 4. 決定

**開 Day 28d「Quantization-Aware Training from Scratch」。**

時序模型列為下一候選(工程量大、值得獨立一輪 loop,暫緩);增強不開全課,以小型修
補處理(Day 25 Hints 補標籤感知增強指引)。另補 Day 27 stateful-export 兩句說明(A2
的猜測點)。

## 5. 驗證計畫

課程建成後,由 S3(最迷失者)研讀新課重考 Section C;通過標準:C 節滿分且不再出現
「用猜的」來源標注。

## 6. 驗證結果(re-probe,課程建成後)

S3 研讀 Day 28d(僅正文 + Hints,一如其 persona)重考 Section C:**9.5/10**。

分數與初考持平,但組成本質不同:初考中自承「用猜的」的部分(梯度如何穿過 round()、
QAT 在訓練迴圈中的具體位置)全數變為有出處的正確作答 —— 批改逐句核對,每條「來源」
引文皆能在 Day 28d 原文找到;無危險型錯誤。S3 自述:「終於懂了 STE 的把戲 —— 前向用
階梯、反向假裝是恆等函數。」

殘留的 0.5 分(C1 未點名 static 為另一個更強的 flavor)為連續兩次的相同遺漏,但同一
知識在 C2 答對 —— 屬探測題 C1 的措辭引導問題(B 類,問句聚焦 QAT 使學生忘記先列舉),
非教材缺口,不再迭代。

**結論:缺口關閉,本輪 loop 收斂。** 下一候選:時序模型(獨立一輪 loop,工程量較大)。

## 7. 時序模型課(Day 28e)與 Section A 驗證結果

依第 6 節預告,下一候選「時序模型」已開課:**Day 28e — Temporal Models: Seeing Motion**。
可行性實驗先行:合成「車道線以固定速度橫移、標籤 = 速度」任務上,單幀模型 val MAE
0.42(≈瞎猜,證明資訊不在單張 frame 裡),frame-stack 0.011、recurrent(GRUCell)
0.010。課程含 export-honest 的顯式狀態 `step(frame, h) → (pred, h_new)`、`init_state`
歸零(每趟行程重置)、以及「序列資料的 shuffle 單位是 clip」;評分器驗證 step 純函數
性與 forward ≡ 手動串接 step(解答 7/7 通過,CPU 約 9 秒)。

S3 研讀新課後重考 Section A:**10/10**。初考的三個猜測區(第二種時序機制的具體架構、
stateful export 機制、行程開始時的狀態重置)全部變為可逐句對回 Day 28e / Day 27 新段
落的正確作答;「用猜的」標注歸零;無危險型錯誤。S3 殘留的自述模糊點(C++ runtime 實
際存放 h 的程式碼樣貌、重送 T 幀 vs 單幀的成本比較)分別屬 Day 29 實地導覽範圍與
Day 28e Check-yourself 既有題目,不再迭代。

**結論:三個進階缺口(QAT、時序、增強)全數處置完畢,gap-probe loop 全面收斂。**
